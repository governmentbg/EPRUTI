@using Ais.Office.Areas.Admin.Controllers
@using Ais.Utilities.Extensions
@using Ais.WebUtilities.Extensions
@{
    var clientInfo = Url.DynamicAction(nameof(ClientsController.MenuInfo), typeof(ClientsController));
}
<script id="CustomerTemplate" type="text/x-kendo-template">
    @{
        if (clientInfo.IsNotNullOrEmpty())
        {
            @:#if(CustomerId){#<a href="@clientInfo?clientId=#:CustomerId#" class="js-trigger-popup" title="#:Customer#">#:Customer#</a>#}else{# #:Customer# #}#
        }
        else
        {
            @:#:Customer#
        }
    }
</script>
<script id="CustomerNumberTemplate" type="text/x-kendo-template">
    @{
        if (clientInfo.IsNotNullOrEmpty())
        {
            @:#if(CustomerId){#<a href="@clientInfo?clientId=#:CustomerId#" class="js-trigger-popup" title="#:Customer#">#:EgnBulstat#</a>#}else{# #:EgnBulstat# #}#
        }
        else
        {
            @:#:EgnBulstat#
        }
    }
</script>
<script id="CustomerKnikTemplate" type="text/x-kendo-template">
    @{
        if (clientInfo.IsNotNullOrEmpty())
        {
            @:#if(CustomerId){#<a href="@clientInfo?clientId=#:CustomerId#" class="js-trigger-popup" title="#:Customer#">#:Knik#</a>#}else{# #:Knik# #}#
        }
        else
        {
            @:#:Knik#
        }
    }
</script>