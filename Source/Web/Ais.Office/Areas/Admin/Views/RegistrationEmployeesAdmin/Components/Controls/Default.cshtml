@using Ais.Office.Areas.Admin.Controllers;
@using Ais.WebUtilities.Extensions
@using Microsoft.Extensions.Localization
@inject IStringLocalizer Localizer
@{
    var controllerType = typeof(RegistrationEmployeesAdminController);
    if (this.Url.HasRightsToAction("Upsert", controllerType))
    {
        <a class="k-small k-button edit-employeeadmin-js" data-searchqueryid="@ViewBag.SearchQueryId" title="@Localizer["Edit"]">
            <i class="k-i-edit k-icon k-font-icon"></i>
        </a>
    }
}